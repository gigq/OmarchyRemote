pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = 'OmarchyRemote'
include ':app'
if (providers.gradleProperty('withQaReceiver').isPresent()) {
    include ':qa-share-receiver'
}
